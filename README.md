# Workload Management Tool - Worker application

## Getting Started

### Pre-requisites

Install dependencies, run migrations and launch the app:

```
yarn install
yarn run migrations
yarn run start
```
