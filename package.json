{
  "name": "wmt-worker",
  "version": "0.1.0",
  "description": "WMT Async worker application",
  "main": "start.js",
  "repository": "https://github.com/ministryofjustice/wmt-worker.git",
  "author": "Tom Swann",
  "license": "MIT",
  "scripts": {
    "start": "node start.js",
    "test": "standard && mocha --recursive -u exports test/unit/",
    "test-unit": "mocha --recursive -u exports test/unit/",
    "test-integration": "mocha --recursive -u exports test/integration/ --timeout 10000",
    "test-all": "yarn test && yarn test-integration",
    "migrate-app": "knex migrate:latest --env app",
    "seed-app": "node ./cleanup.js && knex seed:run --env app",
    "rollback-app": "knex migrate:rollback --env app",
    "migrate-stg": "knex migrate:latest --env staging",
    "rollback-stg": "knex migrate:rollback --env staging",
    "reset-db": "yarn migrate-app && yarn seed-app"
  },
  "standard": {
    "globals": [
      "describe",
      "before",
      "beforeEach",
      "after",
      "afterEach",
      "it"
    ],
    "ignore": [
      "data/**"
    ]
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.2.0",
    "proxyquire": "^1.7.11",
    "sinon": "^1.17.6",
    "sinon-bluebird": "^3.1.0",
    "standard": "^9.0.2"
  },
  "dependencies": {
    "bluebird": "^3.5.0",
    "bunyan": "^1.8.9",
    "bunyan-prettystream": "^0.1.3",
    "cron": "^1.2.1",
    "knex": "^0.13.0",
    "lodash": "^4.17.4",
    "mssql": "3.3.0",
    "wmt-probation-rules": "ministryofjustice/wmt-probation-rules.git#feature/WMT-417/create-workload"
  }
}
